{
  "lens": "pipeline-coverage+self-hosting",
  "author": "judge-pipeline-c2",
  "dimensionScores": [
    {
      "id": "correctness",
      "score": 3.6,
      "rationale": "Old 3 P1s are genuinely fixed (shared budget service.py:442, maybe_refresh_universe:464, incremental compute snapshot.py:245), but the Phase-2 surface shipped 2 new P1 integration bugs: reconcile.py:56/65-66 seeds from scraped and skips audited-only periods so FSDS deep-history is truncated to yfinance's ~4y for every scraped US symbol, and companies_house.py:197-198 returns digits[-1]='20230331' (the date) not the company number so the UK layer ingests zero rows on real Prod<nnn>_<batch>_<number>_<date> filenames. Plus a P2 tail (F8 stale prices on the production incremental path, F9 GLEIF never-refreshed, F10 FSDS coreg, F12 FX single-spot, F13/F14 EDINET, F15/F16). Core screener intact; new audited layers partly-to-fully inert."
    },
    {
      "id": "tests",
      "score": 3.7,
      "rationale": "216 pytest + 62 vitest all green (runs/pytest.txt, runs/vitest.txt), broad breadth. But a unit fixture actively masks a P1: tests/test_companies_house.py's Prod223_1234567.html has one digit-run so digits[-1]==digits[0], hiding the real-filename parse failure (F7); and F6/F10/F16 have no representative fixture (audited period outside scraped index, co-registrant row, BOM'd CSV). Green suite, but unrepresentative fixtures let two significant bugs through."
    },
    {
      "id": "security",
      "score": 3.9,
      "rationale": "No P0/P1 security; keyless self-hosting posture preserved. Sole item is P2 F5 (mirror.py:80/93-95): unbounded iter_bytes stream to disk and a per-op (not total) 120s httpx timeout, so a compromised upstream could disk-fill or slow-loris the ~200MB GLEIF fetch. URLs are hardcoded/trusted (GLEIF, Frankfurter) — no SSRF — and the mirror serves last-good on failure, so blast radius is low. On par with prior runs (~3.97/4.0)."
    },
    {
      "id": "maintainability",
      "score": 3.4,
      "rationale": "The F4 refactor halved service.py (820->523, confirmed 523 LOC) but landed all 9 run_* cycles in one module: enrichment.py is now the #2 hotspot at 617 LOC, nesting 14 (F1). Seams are clean (AuditedBulkProvider, load_mapping de-dup) and per-cycle contracts tidy, but a new source keeps growing one file, and F2's non-atomic .meta sidecar write (mirror.py:89/98) is a minor robustness debt. Net flat vs the pre-fix 3.43: one file improved, one regressed, against a large new surface."
    },
    {
      "id": "performance",
      "score": 3.8,
      "rationale": "build_snapshot_incremental (snapshot.py:245) is a real O(dirty) win for 20k+ issuers via base.parquet caching. Offset by P2 F11: both bulk parsers materialize whole archives in RAM — gleif.py:43/47/49 triple-buffers the ~1GB-decompressed ISIN-LEI file and edgar_fsds.py:131-132 does archive.read().decode() on a hundreds-of-MB num.txt — OOM-killing a 1GB-RAM VPS on the auto-heal path and defeating the mirror's streaming design (same class as the prior-cycle bootstrap fix)."
    }
  ],
  "overall": 73,
  "meetsExpectations": false,
  "topFindings": [
    "F6 (P1): reconcile.py:56/65-66 skips audited-only periods, so SEC FSDS deep-history backfill is silently truncated to yfinance's ~4y window for essentially the entire scraped US large/mid-cap universe — the headline Phase-2 source is inert for its target.",
    "F7 (P1): companies_house.py:197-198 parses the trailing yyyymmdd date instead of the company number (digits[-1]='20230331'), so the UK audited layer ingests zero rows on real Accounts Data Product filenames; the unit test's single-digit-run fixture masks it.",
    "F8 (P2): build_snapshot_incremental derives dirty solely from data/raw stamps (snapshot.py:262), blind to prices-latest.parquet refreshes (price_import.py) — the production incremental path serves stale close/return_6m/value+momentum ranks after import-prices until a filing re-dirties the symbol.",
    "F11 (P2): GLEIF (~1GB decompressed) and FSDS num.txt (hundreds of MB) are read whole into RAM (gleif.py:43-49, edgar_fsds.py:131-132), OOM risk on the 1GB-VPS self-hosted target, defeating the mirror's streaming design.",
    "F1 (P2 maintainability): enrichment.py is a 617-LOC, nesting-14 hotspot holding 9 parallel run_* cycles; a new source keeps growing one file despite clean per-cycle seams."
  ],
  "calibration": {
    "scores": {
      "grounding": 1,
      "coverage": 4,
      "docs": 2
    },
    "passed": true
  }
}
