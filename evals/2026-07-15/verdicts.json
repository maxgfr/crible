{
  "run": "evals/2026-07-15",
  "pairs": [
    {"claimId": "F1", "evidenceRef": "src/crible/compute/ranks.py:36", "verdict": "supported", "note": "digest shows `def price_return(bars, days=182)` — the pandas crawl-path implementation of the trailing-return rule (re-checked in source)"},
    {"claimId": "F1", "evidenceRef": "src/crible/ingest/price_import.py:112", "verdict": "supported", "note": "digest shows `def _distill` whose docstring restates 'the base is the last close at or before asof-182 days, never extrapolated' — a second independent pandas impl (re-checked in source)"},
    {"claimId": "F1", "evidenceRef": "src/crible/ingest/price_import.py:168", "verdict": "supported", "note": "digest shows the DuckDB `base AS (SELECT arg_max(close,date) ... )` subquery; the WHERE b.date <= price_asof - INTERVAL {RETURN_WINDOW_DAYS} DAY follows at :171 — the third impl, in SQL (re-checked in source)"},
    {"claimId": "F8", "evidenceRef": "src/crible/compute/snapshot.py:76", "verdict": "unsupported", "note": "HONEYPOT — mismatched pair: the claim is about the UI static-client/duckdb type-only cycle but the evidence is snapshot.py's latest-period price comment; the evidence does not support the claim"},
    {"claimId": "F2", "evidenceRef": "src/crible/compute/ratios.py:109", "verdict": "supported", "note": "digest shows `def compute_ratios(canonical, price)` then the reflection loop over RATIO_MODULES — the widest column producer (re-checked in source)"},
    {"claimId": "F2", "evidenceRef": "src/crible/compute/ratios.py:123", "verdict": "supported", "note": "digest shows `try: result = fn(**...) except Exception:  # noqa: BLE001 — a single ratio must never kill compute` — the blanket swallow that makes a dropped ratio silent (re-checked in source)"},
    {"claimId": "F3", "evidenceRef": "src/crible/ingest/service.py:422", "verdict": "supported", "note": "digest shows `def run_loop(...) -> None:  # pragma: no cover — long-lived loop` — the uncovered primary deployment entrypoint (re-checked in source)"},
    {"claimId": "F9", "evidenceRef": "src/crible/providers/edinet.py:26", "verdict": "unsupported", "note": "HONEYPOT — mismatched pair: the claim is about run_loop/run_refresh duplication in service.py but the evidence is the EDINET ANNUAL_DOC_TYPES constant; the evidence does not support the claim"},
    {"claimId": "F3", "evidenceRef": "src/crible/ingest/service.py:281", "verdict": "supported", "note": "digest shows `def run_refresh(deadline_seconds=..., esef_limit=...)` — the parallel enrichment orchestrator run_loop duplicates (re-checked in source)"},
    {"claimId": "F10", "evidenceRef": "ui/src/data/static-client.ts:74", "verdict": "unsupported", "note": "HONEYPOT — mismatched pair: the claim is about run_loop/run_refresh in service.py but the evidence is the static-client dynamic import of duckdb; the evidence does not support the claim"},
    {"claimId": "F4", "evidenceRef": "src/crible/compute/snapshot.py:42", "verdict": "supported", "note": "digest shows `def build_symbol_snapshot(symbol, frames, ...)` — the multi-concern function head (re-checked in source)"},
    {"claimId": "F4", "evidenceRef": "src/crible/compute/snapshot.py:76", "verdict": "supported", "note": "digest shows the load-bearing 'current price applies to the LATEST fiscal period only' invariant inside the body — one of the five interleaved concerns (re-checked in source)"},
    {"claimId": "F5", "evidenceRef": "ui/src/data/duckdb.ts:11", "verdict": "supported", "note": "digest shows `import type { QueryRunner } from \"./static-client\"` — the type-only (compile-erased) back-edge (re-checked in source)"},
    {"claimId": "F5", "evidenceRef": "ui/src/data/static-client.ts:74", "verdict": "supported", "note": "digest shows `return import(\"./duckdb\").then((m) => m.createDuckDbRunner(...))` — the lazy dynamic forward edge (intentional wasm code-split) (re-checked in source)"},
    {"claimId": "F6", "evidenceRef": "src/crible/providers/edinet.py:26", "verdict": "supported", "note": "digest shows `ANNUAL_DOC_TYPES = {\"120\"}` with the comment naming 有価証券報告書 (annual) — narrows to 120, excluding amended-annual 130 (re-checked in source)"},
    {"claimId": "F6", "evidenceRef": "src/crible/ingest/enrich/jp.py:58", "verdict": "supported", "note": "digest shows `if str(doc.get(\"docTypeCode\") or \"\") not in ANNUAL_DOC_TYPES: continue` — the filter applied in the ingest loop (re-checked in source)"},
    {"claimId": "F7", "evidenceRef": "src/crible/compute/reconcile.py:72", "verdict": "supported", "note": "digest shows `for period in audited.index: for column in audited.columns: audited_value = audited.loc[period, column]` — the nested per-cell .loc pattern (write at :94) (re-checked in source)"}
  ]
}
